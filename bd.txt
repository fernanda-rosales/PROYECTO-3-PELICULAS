
create table TP_PELICULAS(
  CODIGO NVARCHAR2(20)        NOT NULL,
  TITULO NVARCHAR2(40)        NOT NULL,
	DIRECTOR NVARCHAR2(30)       NOT NULL,
  ACTOR_PRIN NVARCHAR2(20),
  GENERO NVARCHAR2(20)      NOT NULL,
	PRECIO FLOAT              NOT NULL,
  IMAGEN BLOB               NOT NULL,
  CONSTRAINT PK_TP_PELICULAS PRIMARY KEY(CODIGO)
);
 
 
 drop table TP_PELICULAS;
 
 
 
 
 
 
SELECT * FROM Imagenes;

SELECT * FROM TP_PELICULAS;